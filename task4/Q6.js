//6. Take 2 variables and compare them using == and === such that result of both the comparisons operation is not equal.

let x=5;
let y=9;

console.log("checking x=y: ",x==y);
console.log("checking using(===) it checks type to x=y: ",x===y);
